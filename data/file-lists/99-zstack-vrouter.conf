# VRouter NAT and policy routing can use asymmetric paths, so reverse-path
# filtering must remain disabled on current and newly created interfaces.
net.ipv4.conf.all.rp_filter = 0
net.ipv4.conf.default.rp_filter = 0
net.ipv4.conf.*.rp_filter = 0
